#!groovy
@Library(my-shared-library@master) _

pipeline {
    agent any

    stages {
        stage ('first stage') {
            echo 'Hello world'
            }
        stage ('second stage') {
            echo 'welcome library'
            }
    }
}
